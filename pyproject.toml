[project]
name = "ai-chat-app"
version = "0.1.0"
description = "AI Multi-Model Chat Application — University Project"
requires-python = ">=3.14"
dependencies = [
    "nicegui>=2.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
